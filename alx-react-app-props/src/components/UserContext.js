import { createContext } from 'react';

// Create the context - this is like creating a "channel" for data
export const UserContext = createContext();

import { createContext } from 'react';

const UserContext = createContext();

export default UserContext; 